---
// Mini-Manifest zwischen About und Pillars
// Quelle: Buch Kap. 11 „Unsere größte Krise" — „Wahrgenommen-Sein öffnet die Türe."
// Plus Workbook „Dein Leben lesen lernen" Kap. 9 — „Ohne Lösung wirksam helfen"
// Plus Sam-Geschichte (Südafrika-Interview)
---

<section class="manifesto-fokus" aria-label="Manifest">
  <div class="container narrow">
    <p class="m-problem">
      Warum wiederholen sich diese Themen ständig? Weil wir gelernt haben,
      sie wegzudrücken — und weil uns niemand wirklich zuhört.
    </p>

    <p class="m-line m-1">Dabei reicht eines:</p>
    <p class="m-line m-2"><em>wahrgenommen zu werden.</em></p>
    <p class="m-line m-3">Ohne Rat. Ohne Urteil. Ohne Zeitdruck.</p>

    <p class="m-wirkung">
      Wenn dir jemand wirklich zuhört, sprichst du Dinge aus,
      die dir selbst neu sind — und etwas in dir kommt in Bewegung.<br>
      <em>Genau da beginnst du, dein Leben zu lesen.</em>
    </p>
  </div>
</section>

<style>
  .manifesto-fokus {
    background: var(--bg-white);
    padding: clamp(48px, 7vw, 80px) 0;
    text-align: center;
    position: relative;
  }
  .manifesto-fokus::before,
  .manifesto-fokus::after {
    content: "";
    display: block;
    width: 40px;
    height: 1px;
    background: var(--orange);
    opacity: 0.5;
    margin: 0 auto 36px;
  }
  .manifesto-fokus::after {
    margin: 36px auto 0;
  }
  .m-line {
    margin: 0;
    line-height: 1.3;
    color: var(--blue-deep);
  }
  .m-1 {
    font-family: var(--font-body);
    font-size: clamp(18px, 1.8vw, 22px);
    color: var(--ink-soft);
    margin-bottom: 6px;
  }
  .m-2 {
    font-family: var(--font-display);
    font-style: italic;
    font-size: clamp(26px, 3.2vw, 40px);
    line-height: 1.2;
    color: var(--blue-deep);
    margin-bottom: 18px;
    letter-spacing: -0.01em;
  }
  .m-3 {
    font-family: var(--font-display);
    font-style: italic;
    font-size: clamp(18px, 1.7vw, 22px);
    color: var(--rose);
  }
  .m-problem {
    font-family: var(--font-body);
    font-size: clamp(17px, 1.5vw, 20px);
    line-height: 1.7;
    color: var(--ink-soft);
    max-width: 640px;
    margin: 0 auto 44px;
  }
  .m-wirkung {
    font-family: var(--font-body);
    font-size: clamp(17px, 1.5vw, 20px);
    line-height: 1.7;
    color: var(--ink-soft);
    max-width: 640px;
    margin: 44px auto 0;
  }
  .m-wirkung em {
    font-family: var(--font-display);
    font-style: italic;
    color: var(--blue-deep);
  }

  @media (max-width: 720px) {
    .manifesto-fokus::before,
    .manifesto-fokus::after { margin-bottom: 24px; }
    .manifesto-fokus::after { margin: 24px auto 0; }
  }
</style>
